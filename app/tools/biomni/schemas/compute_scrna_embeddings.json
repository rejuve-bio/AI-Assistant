{
  "name": "compute_scrna_embeddings",
  "module": "genomics",
  "description": "Compute low-dimensional embeddings for scRNA-seq data using scVI, Harmony, or PCA",
  "usage": "from app.tools.biomni.genomics import compute_scrna_embeddings; result = compute_scrna_embeddings('input/cells.h5ad', method='scvi', output_dir='output/')",
  "parameters": {
    "adata_path": "str — path to .h5ad AnnData file",
    "method": "str — embedding method: 'scvi', 'harmony', 'pca' (default 'scvi')",
    "output_dir": "str — directory for output files (default 'output/')"
  },
  "returns": "dict with keys: embedding_shape, method, output_files, umap_computed",
  "tags": ["scrna", "embedding", "dimensionality reduction", "scvi", "harmony", "umap", "pca", "batch correction", "single cell"]
}